{
  "name": "pettygo-backend",
  "version": "0.1.0",
  "description": "PettyGo ERP Backend (NestJS)",
  "main": "main.ts",
  "scripts": {
    "start": "nest start",
    "start:dev": "ts-node --transpile-only -r tsconfig-paths/register main.ts",
    "build": "nest build"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "ts-node": "^10.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.0"
  }
}
